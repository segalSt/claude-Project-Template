# TODO — состояние шаблона · Template status

## Миграция шаблона на split-plan раскладку — ЗАВЕРШЕНА

Все файлы шаблона приведены к текущей раскладке (plan.md = индекс шагов;
тело+задачи+QA в `plan/step-<N>.md`, закоммиченные — `plan/archive/`):

- [x] `templates/plan-template.md` (RU+EN) — порог разбиения + раскладка.
- [x] `templates/actualization-rules.md` (RU+EN) — правила 10-12
  (DoD, один источник/normative-illustrative/порог церемонии, авто-стражи).
- [x] `templates/commands.md` (RU+EN) — `-plan`/`-QA`/`-старт`/`-new`
  работают при обеих раскладках (монолит / split), источники — из файла шага.
- [x] `templates/context-file-template.md` (RU+EN) — раскладка плана указана.
- [x] `new-project-setup.md` (RU+EN) — Шаг 2: старт с монолита, split по порогу.
- [x] Bootstrap-триггер миграции — ОТМЕНЁН (миграция сделана явно, не по бутстрапу).

## Новое

- **`migration.md` (RU+EN)** — интерактивная миграция существующего проекта
  к структуре шаблона по команде **`-миграция`/`-migrate`** (спросить
  папку+главный файл+язык → анализ → уточнения → миграция → отчёт).
  Сценарии A (adopt) и B (split монолита); приготовления, команды и грабли
  (ASCII-only PowerShell, memory-map lock, предохранитель, git-проверка).
  Токен `-миграция`→`-migrate` добавлен в `SYNC.md`.

## Адаптации из сравнения с tagsExpert (2026-08-11)

По итогам сравнения структуры шаблона с реальным проектом (tagsExpert) в
шаблон перенесены три улучшения (RU + EN-зеркало):

- [x] `templates/devHistory.md` (RU+EN) — добавлена масштабируемая
  раскладка (индекс сессий + последняя инлайн, старые сессии →
  `history/session-<N>.md`), по аналогии с порогом разбиения
  `plan-template.md`.
- [x] `templates/actualization-rules.md` (RU+EN) — новое правило 14:
  изменение логики/интерфейса обязано в том же заходе проверить/обновить
  автотесты и ручной QA-чек-лист соответствующего шага.
- [x] `templates/qa-findings.md` (RU+EN, новый файл) — баги приложения,
  таблица id/экран/симптом/ожидаемое/статус/шаг; добавлен в `README.md`
  (раздел "Файлы") и в `new-project-setup.md` (Шаг 2 структура + Шаг 3
  список копирования) в обеих копиях.
- [x] `SYNC.md` — счётчик файлов `templates/` обновлён 9→10 (RU+EN блоки).

Отклонено (см. обсуждение): handoff-механизм — это отдельный скилл (Matt
Pocock), не относится к этому шаблону; device-bridge-специфичные грабли
(git-lock на смонтированной Windows-папке, отсутствие dotnet в песочнице)
— оставлены в проекте (tagsExpert), слишком специфичны для общего шаблона.

Дальнейших открытых пунктов по шаблону нет. Правило синка RU→EN — `SYNC.md`.
